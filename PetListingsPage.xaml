<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Lab_2_layout.PetListingsPage"
             Title="Available Pets"
             BackgroundColor="{AppThemeBinding Light=White, Dark=#1C1C1E}">
    <Grid RowDefinitions="Auto, *">
        <Label Text="Pets Available for Adoption"
               FontSize="24"
               FontAttributes="Bold"
               HorizontalOptions="Center"
               Margin="0,20,0,10"
               TextColor="{AppThemeBinding Light=Black, Dark=White}"/>

        <CollectionView Grid.Row="1" ItemsSource="{Binding Pets}" SelectionMode="Single" SelectionChanged="OnPetSelected">
               <CollectionView.ItemsLayout>
            <GridItemsLayout Orientation="Vertical" Span="2" HorizontalItemSpacing="10" VerticalItemSpacing="10" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame HasShadow="True"
                           CornerRadius="10"
                           Padding="10"
                           BackgroundColor="{AppThemeBinding Light=White, Dark=#333333}"
                           Margin="5">
                        <VerticalStackLayout>
                            <Image Source="{Binding ImageUrl}"
                                   Aspect="AspectFill"
                                   HeightRequest="150"
                                   WidthRequest="150"
                                   HorizontalOptions="Center"
                                   VerticalOptions="CenterAndExpand" />
                            <Label Text="{Binding Name}"
                                   FontAttributes="Bold"
                                   FontSize="18"
                                   HorizontalOptions="Center"
                                   Margin="0,10,0,0"
                                   TextColor="{AppThemeBinding Light=Black, Dark=White}" />
                            <Label Text="{Binding Breed}"
                                   FontSize="14"
                                   HorizontalOptions="Center"
                                   TextColor="{AppThemeBinding Light=Black, Dark=White}" />
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>